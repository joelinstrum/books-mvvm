import React from "react";
import { Header, AppWrapper } from "../../UI-components";

const AppPage = () => {
  return (
    <AppWrapper>
      <Header />
    </AppWrapper>
  );
};

export default AppPage;
